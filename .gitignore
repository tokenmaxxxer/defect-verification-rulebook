.DS_Store
.idea/

# Local settings backup produced by a CLI-less install.sh run against this
# repo itself (e.g. during manual testing).
*.bak

# The verify role's state file is runtime state produced during a verify
# session, not a design artifact; it should never be committed into the
# plugin repository itself.
verify-record.md
